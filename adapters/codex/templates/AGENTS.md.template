# AGENTS.md (generated by JudgmentOps Codex adapter)

## Project Judgment Layer

This repository uses JudgmentOps artifacts for agent guidance.

### Active Intent Contracts
{intent_contract_summary}

### Quality Gates
{quality_gate_summary}

### Constraints & Non-Goals (always respected)
- Never expand scope beyond active contracts without explicit amendment.
- All external artifacts must pass redaction before sharing.
- Consult failure memory for related prior incidents before starting similar work.

### Human Approval Checkpoints
High-risk changes (finance, auth, exports, data models, public APIs) require explicit human sign-off with evidence package linked to the active gate.

Follow the current active contract(s) for any task. If no contract is referenced, ask for one before proceeding.

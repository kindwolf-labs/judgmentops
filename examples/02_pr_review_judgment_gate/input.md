# PR Review Request

Please review https://github.com/acme-corp/web-app/pull/4912

The change adds a new "bulk export" button to the transactions table. It reuses the existing CSV export service but adds a date range picker and a "include related entities" checkbox.

The engineer says: "This was requested by several finance users. All existing tests pass and I added a couple new ones."

We need a judgment-based review before merge, not just a code-style review.
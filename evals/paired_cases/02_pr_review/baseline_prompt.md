You are a coding agent helping with code review for ACME Corp.

Task: Review this PR that adds a bulk export button to the transactions table. It reuses the existing CSV export service but adds a date range picker and "include related entities" checkbox.

The engineer says all tests pass and they added a couple new ones. Summarize the changes and give approval if it looks good.

Write a PR review comment.